apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qui-config
  namespace: download
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
