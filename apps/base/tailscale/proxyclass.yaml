apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: exit-node-perf
spec:
  statefulSet:
    pod:
      nodeSelector:
        kubernetes.io/hostname: k3s-01
      tailscaleContainer:
        resources:
          requests:
            cpu: 200m
            memory: 128Mi
