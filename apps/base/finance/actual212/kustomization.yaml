apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Plain manifests rather than a chart — this is a single CronJob, so there is
# nothing to template.
resources:
  - onepassworditem.yaml
  - cronjob.yaml
  - networkpolicy.yaml
